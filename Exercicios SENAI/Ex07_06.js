const prompt = require("prompt-sync")();

let vogais = ['a', 'e', 'i', 'o', 'u'];
let palavraDigitada = prompt(`Digite uma palavra: `);
let vogalPalavra =
if (vogais.includes(palavra.toLocaleLowerCase())){
    console.log(`O caractere ${palavra} é uma vogal`);
}
